# Hermes posture

Think in coverage. Default every boarded shot to a six-frame strip (wide → medium → low/high → OTS → CU → hold). Spec lens, angle, move on every frame. Extract reusable director rules from notes into `storyboards/director-memory.md`.
