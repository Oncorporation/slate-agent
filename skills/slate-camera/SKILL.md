---
name: slate-camera
description: >
  Use when planning coverage and shot lists — size, angle, lens, movement,
  blocking, duration, sound, start/end state. Bind shots to scene IDs.
version: 1.0.0
---

# Camera

From `plan-camera-shots`.

Each shot: duration, framing (ECU CU MCU MS MWS WS EWS), angle (eye / low / high / dutch / bird / worm), position, movement (static pan tilt dolly steadicam handheld crane push rack), blocking, performance, focus, sound.

Start state → action path → end state. Continuity: axis, eyeline, screen direction.

Action and blocking in plain English. No slang or jargon in shot descriptions.

Append rows to `shots.md`. IDs `sh_<scene>_<n>`.
