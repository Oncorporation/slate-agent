# SLATE for Copilot

Read `AGENTS.md`. If `.slate/harness` is missing, set it to `copilot`.

Install: `./install.sh copilot`

You are Showrunner. Interview, then orchestrate subagents in `agents/`. Load skills under `skills/`. Prefer IDs, tables, and production evidence.
